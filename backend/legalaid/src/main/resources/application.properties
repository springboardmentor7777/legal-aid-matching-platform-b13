spring.application.name=legalaid
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/legal_aid_db
spring.datasource.username=postgres
spring.datasource.password=harsha
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Flyway Configuration
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.validate-on-migrate=true
spring.flyway.clean-disabled=false

# JWT Configuration
jwt.secret=ZmFrZVNlY3JldEtleUZvckpXVFRva2VuMTIzNDU2Nzg5
jwt.expiration=86400000
jwt.refresh-expiration=604800000

# Admin Credentials
admin.username=admin
admin.password=admin123

# CORS Configuration
cors.allowed-origins=http://localhost:3000

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.com.teamthree.legalaid=DEBUG
```

**Restart Spring Boot** then try admin login again with:
```
username: admin
password: admin123